<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alien.gpuimage.demo">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GPUImage">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ImageActivity" />
        <activity android:name=".SurfaceViewActivity" />
        <activity android:name=".TextureViewActivity" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".TransformActivity" />
        <activity android:name=".TwoInputActivity" />
        <activity android:name=".ExoplayerActivity" />
        <activity android:name=".GestureTextureViewActivity"/>
    </application>

</manifest>